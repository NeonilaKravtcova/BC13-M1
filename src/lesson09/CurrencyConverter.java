package lesson09;

public class CurrencyConverter {
    public static void main(String[] args) {
        //Реализовать метод, который принимает название валюты (например, “EUR”,”RUR”,”USD”,”GPB”) и выдает курс к
        //доллару. Естественно, “USD” возвращает 1.0
        //System.out.println(findCourse(currencyName: "EUR"));
        //System.out.println(findDoubleCourse("EUR", "RUB"));
      /*  System.out.println(converter(1000, "RUB", "EUR"));
    }

    public static double converter(double amount, String inputCurrency, String outputCurrency) {
        return amount * findDoubleCourse(inputCurrency, outputCurrency);
    }
}

    public static double findDoubleCourse(String firstCurrency, String secondCurrency) {
        return findCourse(firstCurrency) / findCourse(secondCurrency);
    }

    public static double findCourse(String currencyName) {
        currencyName.toUpperCase();
        double course = 0.0;
        switch (currencyName) {
            case "EUR":
                course = 1.13;
                break;
            case "USD":
                course = 1;
                break;
            case "GBP":
                course = 1.26;
                break;
            case "RUB":
                course = 1.13;
                break;


        }
        return course;
    }*/
    }
}